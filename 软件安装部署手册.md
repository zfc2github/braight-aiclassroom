# 软件安装部署手册

> 适用项目：和光未来 AI 教学平台（braight-aiclassroom）
> 包含：CMS 后台管理系统、教育官网（education.braightidea.com）、以及围绕官网挂载的 scratch3 / 智慧乐队 / 你画我猜 / 看见声音 / 神经网络可视化 / 大模型代理等子服务。

---

## 1. 文档说明

本手册依据项目根目录下的两份 Nginx 服务配置文件编写：

- `nginx.cms.education.braightidea.conf` —— CMS 后台管理站点
- `nginx.education.braightidea.conf` —— 教育官网（主站）及其子服务

源码与部署目录的对应关系，均以 Nginx 配置中的真实路径为准。

### 1.1 代码仓库结构（本机源码目录）

| 源码目录 | 用途 | 技术栈 |
| --- | --- | --- |
| `braightidea-edu-aiclassroom` | CMS 后台整体工程（后端 + 前端） | Spring Boot / Vue2 |
| `braightidea-education` | 教育官网前端 | React 18 + Vite + TS |
| `eduStatics` | 官网静态资源（图片/文件直链） | 静态 |
| `aiclassroom` | Scratch3 图形化编程等静态站点包 | 静态 HTML |
| `semi-conductor` | 智慧乐队（semiconductor） | Parcel + TensorFlowJS |
| `quickdraw-online` | 你画我猜（quickdraw） | Python + 深度学习 |
| `creatability-seeing-music` | 看见声音（seeingmusic） | npm + event handler |
| `Neural-Network-Visualisation` | 神经网络可视化（nnv） | 静态 Web |
| `nodejs-proxy-server` | 大模型/云能力聚合代理（nodejsProxy） | Node.js + Express |

> 仓库中其余目录（`face-api.js`、`*-classification-training`、`text*`、`voice*`、`image2image` 等）为模型训练与算法实验代码，不参与线上 Http 服务部署，此处不展开。

---

## 2. 系统架构与拓扑

### 2.1 域名与访问入口

| 域名 | 说明 | 对应 Nginx 配置 |
| --- | --- | --- |
| `https://cms.education.braightidea.com` | CMS 后台管理系统 | `nginx.cms.education.braightidea.conf` |
| `https://education.braightidea.com` | 教育官网主站（含子服务） | `nginx.education.braightidea.conf` |

两个站点均强制 `http → https` 跳转（`return 301 https://$host$request_uri;`）。

### 2.2 服务器与目录映射总览

以教育官网配置 `nginx.education.braightidea.conf` 为准，各子路径映射如下：

| URL 路径 | 类型 | 转发/目录目标 |
| --- | --- | --- |
| `/` | 静态 | `/var/www/braight-aiclassroom/braightidea-education/build`（官网 dist） |
| `/eduStatics/*` | 静态 | `/var/www/braight-aiclassroom/eduStatics/` |
| `/scratch3/*` | 静态 | `/var/www/braight-aiclassroom/braight-scratch3/build/` |
| `/semiconductor/*` | 静态 | `/var/www/braight-aiclassroom/semi-conductor/dist/` |
| `/quickdraw/*` | 反向代理 | `http://172.30.185.39:5000/` |
| `/seeingmusic/*` | 混合 | 静态:`/var/www/.../creatability-seeing-music/build/` + 代理 `http://172.30.185.39:8106/` |
| `/nnv/*` | 反向代理 | `http://172.30.185.39:8000/` |
| `/nodejsProxy/*` | 反向代理 | `http://172.30.185.39:8602/` |
| `/eduServer/*`、`/admin/*` | 反向代理 | `https://172.30.185.38:25172/admin/`（CMS 后端） |
| `/canva-design/*`、`/_ajax/*` | 反向代理 | `https://www.canva.cn`（Canva 嵌入） |

CMS 后台配置 `nginx.cms.education.braightidea.conf` 的映射如下：

| URL 路径 | 类型 | 转发/目录目标 |
| --- | --- | --- |
| `/` | 静态 | `/var/www/braight-aiclassroom/braightidea_edu_aiclassroom/dist`（CMS dist） |
| `/api/*` | 反向代理 | `https://172.30.185.38:25172/admin/`（CMS 后端） |

### 2.3 服务端口清单

| 服务 | 端口 | 部署主机（参考拓扑） |
| --- | --- | --- |
| CMS 后端（Spring Boot, prod） | 25172 | 172.30.185.38 |
| MySQL | 3306 | 172.30.185.38 |
| Redis | 6379 | 172.30.185.38 |
| nodejs-proxy-server（大模型代理） | 8602 | 172.30.185.39 |
| quickdraw（你画我猜 Python） | 5000 | 172.30.185.39 |
| seeingmusic 事件服务 | 8106 | 172.30.185.39 |
| nnv 静态服务 | 8000 | 172.30.185.39 |
| OpenResty（门户/代理） | 80 / 443 | 对外提供访问 |

> 端口/主机号均取自 Nginx 配置，请以实际部署环境为准。

---

## 3. 基础环境准备

以下环境为所有组件共用的前置依赖，建议在两台应用机与 OpenResty 机器上按需安装。

### 3.1 Java（CMS 后端）

CMS 后端基于 Spring Boot，需 JDK 1.8+（项目使用 Lombok、javacv 等，推荐 **JDK 8**）：

```bash
# Ubuntu/Debian
sudo apt update && sudo apt install -y openjdk-8-jdk
java -version
```

### 3.2 Maven（构建 CMS 后端）

```bash
sudo apt install -y maven
mvn -version
```

### 3.3 Node.js / npm

官网前端要求 `node >= 18.17`；CMS 前端（Vue2）因工程较老建议使用 **Node 14/16**（`engines` 声明 `node >=8.9,<=16`）。建议通过 nvm 切换版本：

```bash
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.7/install.sh | bash
nvm install 16 && nvm use 16   # 构建 CMS 前端
nvm install 18 && nvm use 18   # 构建官网前端 / 运行 nodejs-proxy
```

### 3.4 Python（quickdraw 等算法服务）

```bash
sudo apt install -y python3 python3-pip
python3 --version   # >= 3.8
```

### 3.5 MySQL / Redis

- MySQL 8.x，运行于 `172.30.185.38:3306`。
- Redis，运行于 `172.30.185.38:6379`，使用 `database:1`。

安装示例：

```bash
sudo apt install -y mysql-server redis-server
sudo systemctl enable --now mysql redis-server
```

### 3.6 OpenResty（替代 Nginx 作为门户/代理）

本项目使用 **OpenResty**（内置 Nginx + Lua/`set-misc` 等模块）作为 Web 门户与反向代理。尤其在官网配置中使用了 `set_unescape_uri`（需 `ngx_set_misc` 模块），该能力 OpenResty 原生内置，故统一采用 OpenResty。

Debian/Ubuntu 安装 OpenResty：

```bash
# 添加官方软件源后安装
sudo apt update
#（推荐方式）直接拉取官方安装脚本，或手动添加 openresty 源
wget -O - https://openresty.org/package/pubkey.gpg | sudo apt-key add -
echo "deb http://openresty.org/package/ubuntu $(lsb_release -sc) main" \
  | sudo tee /etc/apt/sources.list.d/openresty.list
sudo apt update && sudo apt install -y openresty
```

验证：

```bash
openresty -v
# 服务进程：nginx（openresty）运行于 80/443
```

---

## 4. 数据库与配置初始化（CMS 后端）

CMS 后端数据源与缓存配置位于：

- `braightidea-edu-aiclassroom/admin/src/main/resources/application.yml`（默认端口 25172、上传根路径等）
- `braightidea-edu-aiclassroom/admin/src/main/resources/application-prod.yml`（生产数据源 / Redis / 飞书）

### 4.1 数据库

1. 在 MySQL 创建数据库并授权（库名必须与配置一致）：

```sql
CREATE DATABASE IF NOT EXISTS braightidea_edu_aiclassroom
  DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci;

CREATE USER IF NOT EXISTS 'braightidea'@'%' IDENTIFIED BY '你的数据库密码';
GRANT ALL PRIVILEGES ON braightidea_edu_aiclassroom.* TO 'braightidea'@'%';
FLUSH PRIVILEGES;
```

2. 导入建表脚本（`SQL` 建表文件请从项目 `install/` 目录或 DBA 处获取，导入前先备份线上库）。

3. 核对 `application-prod.yml` 中的连接信息：

```yaml
spring:
  datasource:
    dynamic:
      datasource:
        primary-db:
          url: jdbc:mysql://172.30.185.38:3306/braightidea_edu_aiclassroom?...&serverTimezone=Asia/Shanghai
          username: braightidea
          password: ${DB_PASSWORD}
  redis:
    host: 172.30.185.38
    port: 6379
    database: 1
    password: ${REDIS_PASSWORD}
```

> 生产环境的 `password` 请勿明文提交到代码库，建议改为环境变量注入。

### 4.2 上传目录

`application.yml` 中配置了 `profile: /var/www/braight-aiclassroom/braightidea_edu_aiclassroom/uploadPath`。部署前需创建该目录并授权：

```bash
sudo mkdir -p /var/www/braight-aiclassroom/braightidea_edu_aiclassroom/uploadPath
sudo chown -R www-data:www-data /var/www/braight-aiclassroom/braightidea_edu_aiclassroom/
```

---

## 5. 组件部署步骤

### 5.1 CMS 后端（Spring Boot，端口 25172）

源码：`braightidea-edu-aiclassroom/admin`

```bash
cd braightidea-edu-aiclassroom/admin
# 使用 prod 环境 + 打包 war
mvn clean package -DskipTests -P prod
```

打包产物：`admin/target/admin.war`（`packaging=war`，`finalName=admin`）。

部署到服务器 `172.30.185.38`，可用以下任一方式：

**方式 A：Tomcat 部署（war）**

将 `admin.war` 放到 Tomcat `webapps/`，并保证 Tomcat 以 HTTPS 监听 `25172`（需配置证书，或由 OpenResty 转发）。

**方式 B：Spring Boot 独立运行（jar）**

```bash
cp admin/target/admin.war admin.war
# 启动（默认读取 active=prod）
nohup java -jar admin.war --spring.config.additional-location=classpath:/ \
  > /var/log/admin.log 2>&1 &
```

验证：

```bash
curl -k https://127.0.0.1:25172/admin/ -m 5
# 或 curl -k https://127.0.0.1:25172/api/...
```

> OpenResty 将 `/api/`（CMS）和 `/admin/`（官网）都代理到 `https://172.30.185.38:25172/admin/`，因此后端需支持 HTTPS 访问（可自签证书，或采用 proxy_ssl 免校验策略）。

### 5.2 CMS 前端（admin-ui，Vue2）

源码：`braightidea-edu-aiclassroom/admin-ui`
构建要求：Node 14/16。

```bash
cd braightidea-edu-aiclassroom/admin-ui
npm install          # 或 pnpm install
npm run build:prod   # 产物在 dist/
```

- 生产接口前缀：`VUE_APP_BASE_API = '/api'`（见 `.env.production`）。
- 部署目标目录：`/var/www/braight-aiclassroom/braightidea_edu_aiclassroom/dist`

```bash
sudo mkdir -p /var/www/braight-aiclassroom/braightidea_edu_aiclassroom
sudo rsync -a --delete dist/ /var/www/braight-aiclassroom/braightidea_edu_aiclassroom/dist/
```

访问：`https://cms.education.braightidea.com/`

### 5.3 教育官网前端（braightidea-education，Vite + React）

源码：`braightidea-education`
构建要求：Node 18。

```bash
cd braightidea-education
npm install
npm run build        # outDir = build
```

- 部署目标目录：`/var/www/braight-aiclassroom/braightidea-education/build`

```bash
sudo mkdir -p /var/www/braight-aiclassroom/braightidea-education
sudo rsync -a --delete build/ /var/www/braight-aiclassroom/braightidea-education/build/
```

访问：`https://education.braightidea.com/`

### 5.4 nodejs-proxy-server（大模型/云能力代理，端口 8602）

源码：`nodejs-proxy-server`
部署主机参考：`172.30.185.39`

```bash
cd nodejs-proxy-server
npm install
# 若有缺包可执行项目自带的安装脚本
bash install.sh
# 启动
nohup node app.js > /var/log/nodejsProxy.log 2>&1 &
```

验证：

```bash
curl -s http://127.0.0.1:8602/ -m 5
```

访问路径：`https://education.braightidea.com/nodejsProxy/*`

> 该服务聚合了阿里云（通义/文字识别/图片生成）、百度（OCR/语音/文生图）、MiniMax（音乐/视频）、COSYVOICE 等大模型接口，请确认各云平台 AK/SK 已在 `app.js` 对应文件中正确配置。

### 5.5 quickdraw（你画我猜，端口 5000）

源码：`quickdraw-online`
部署主机参考：`172.30.185.39`
依赖：Python 3.8+。

```bash
cd quickdraw-online
pip install -r requirements.txt
# 放置模型权重与数据集（见 readme.md）后启动
cd interact_html
nohup python3 app.py --port 5000 > /var/log/quickdraw.log 2>&1 &
```

访问路径：`https://education.braightidea.com/quickdraw/`

> OpenResty 对 `/quickdraw/` 配置了 WebSocket 升级，需确保上游服务支持 ws，否则实时识别断开。

### 5.6 seeingmusic（看见声音，静态 + 事件服务 8106）

源码：`creatability-seeing-music`
部署主机参考：`172.30.185.39`

```bash
cd creatability-seeing-music
npm install
# 构建前端
npm run build        # 产物建议放到 build/（nginx 指向 /build/）
# 构建/启动事件服务（项目自带 start.sh）
bash start.sh        # 监听 8106
```

验证：`curl -s http://127.0.0.1:8106/ -m 5`

访问路径：`https://education.braightidea.com/seeingmusic/`

### 5.7 nnv（神经网络可视化，静态 8000）

源码：`Neural-Network-Visualisation`
部署主机参考：`172.30.185.39`

```bash
cd Neural-Network-Visualisation
# 直接使用项目自带的启动脚本
bash start.sh
```

访问路径：`https://education.braightidea.com/nnv/`

### 5.8 智慧乐队 / Scratch3 / 静态资源

- 智慧乐队：`semi-conductor`，访问 `/semiconductor/`。部署时直接使用 `semi-conductor/dist-cn`（已汉化版本）作为产物，覆盖到 `/var/www/braight-aiclassroom/semi-conductor/dist/`。
- Scratch3：静态包 `braight-scratch3/scratch-gui/build/`，访问 `/scratch3/`，直接将产物同步到 `/var/www/braight-aiclassroom/braight-scratch3/build/`。
- 官网静态资源：`eduStatics`，同步到 `/var/www/braight-aiclassroom/eduStatics/`，访问 `/eduStatics/`。

```bash
# 以 eduStatics 为例
sudo mkdir -p /var/www/braight-aiclassroom/eduStatics
sudo rsync -a --delete eduStatics/ /var/www/braight-aiclassroom/eduStatics/
```

---

## 6. OpenResty 配置与部署

### 6.1 证书准备

将域名证书放到证书目录（配置中为 `/var/www/braight-aiclassroom/` 下）：

```bash
sudo ls /var/www/braight-aiclassroom/
# 预期包含：
#   cms.education.braightidea.com.pem / .key
#   education.braightidea.com.pem / .key
```

如在阿里云 / 腾讯云申请证书，请将证书链合并为 `.pem`，私钥保存为 `.key`。若使用自签证书做后端内部通信，可忽略。

### 6.2 安装配置文件

两份站点配置可放入 OpenResty 的 `conf.d` 目录（OpenResty 默认从 `/usr/local/openresty/nginx/conf/nginx.conf` 中 `include conf.d/*.conf;`），放置后重载：

```bash
sudo mkdir -p /usr/local/openresty/nginx/conf/conf.d
sudo cp nginx.cms.education.braightidea.conf /usr/local/openresty/nginx/conf/conf.d/
sudo cp nginx.education.braightidea.conf     /usr/local/openresty/nginx/conf/conf.d/
```

> 若两份配置位于仓库根目录，请先将其部署到服务器，再执行上述命令。若未启用 `conf.d` 的 include，也可直接将两份配置内容合入 `/usr/local/openresty/nginx/conf/nginx.conf` 的 `http{}` 块内。

### 6.3 校验并重载

```bash
/usr/local/openresty/bin/openresty -t
# 或 systemctl reload openresty
```

若 `openresty` 已注册为 systemd 服务：

```bash
sudo systemctl reload openresty
```

### 6.4 关键配置项说明

- **HTTPS 强制跳转**：80 端口 server 块统一 `return 301 https://$host$request_uri;`，规避 iOS 兼容问题。
- **SPA 路由**：CMS 配置 `try_files $uri $uri/ /index.html;` 保证前端路由刷新不 404。
- **跨域**：CMS `/` 与 `/quickdraw` 已配置 CORS 头，业务代码中一般无需额外处理。
- **set-misc 模块**：官网 `/link` 使用 `set_unescape_uri` 解码跳转参数，依赖 `ngx_set_misc`（OpenResty 已内置，无需额外编译）。
- **大连接/长请求**：`/nodejsProxy/`、`/quickdraw/` 已放大代理超时（`proxy_read_timeout`、WebSocket `Upgrade`）。
- **Canva 代理**：`/canva-design/`、`/_ajax/` 代理到 `https://www.canva.cn`，并做了去除压缩与内容替换（隐藏 Canva 版权壳）。

---

## 7. 部署顺序与验证（建议）

建议按依赖从下往上部署：

1. 基础环境（JDK/Maven/Node/Python/MySQL/Redis/OpenResty）
2. 创建数据库并导入脚本，启动 Redis
3. 启动 CMS 后端（:25172）并验证
4. 构建并部署 CMS 前端 dist
5. 构建并部署教育官网 build
6. 部署 nodejs-proxy、quickdraw、seeingmusic、nnv 等子服务
7. 同步静态资源（eduStatics / scratch3 / semiconductor）
8. 安装 OpenResty 配置并重载

### 7.1 冒烟验证清单

| 项目 | 验证命令/地址 | 预期 |
| --- | --- | --- |
| CMS 官网 | `https://cms.education.braightidea.com/` | 后台登录页可打开 |
| CMS 接口 | `https://cms.education.braightidea.com/api/...` | 返回 JSON（非 502/404） |
| 官网首页 | `https://education.braightidea.com/` | 首页正常 |
| 你画我猜 | `https://education.braightidea.com/quickdraw/` | 可进入绘制页 |
| 看见声音 | `https://education.braightidea.com/seeingmusic/` | 页面可加载 |
| 智慧乐队 | `https://education.braightidea.com/semiconductor/` | 页面可加载 |
| 神经网络可视化 | `https://education.braightidea.com/nnv/` | 页面可加载 |

---

## 8. 常见问题

**Q1：CMS 页面打不开 / 接口返回 502？**
检查后端服务是否监听 25172：`curl -k https://127.0.0.1:25172/admin/`；确认 OpenResty 中 `/api/`、`/admin/` 代理的 upstream IP/端口正确。

**Q2：后端 HTTPS 校验失败。**
OpenResty 代理后端使用了 `https://172.30.185.38:25172`，若后端为自签证书，需确认 OpenResty 编译包含 SSL 模块并在后端启用 TLS；或调整代理为 `http://` 并让 Tomcat/应用监听 http。

**Q3：官网刷新 404。**
官网是 React 单页应用，请确认 OpenResty `location / { try_files $uri $uri/ =404; }` 已存在（或按需要改为 `try_files $uri $uri/ /index.html;`）。

**Q4：quickdraw 实时画笔无反馈。**
确认 OpenResty `/quickdraw/` 已配置 `proxy_set_header Upgrade $http_upgrade; proxy_set_header Connection "upgrade";`，且上游 5000 服务 WebSocket 正常。

**Q5：Node 版本导致构建失败。**
Vue2 前端请用 Node 14/16（`nvm use 16`）；官网前端用 Node 18（`nvm use 18`）。勿用同一版本构建两个前端。

**Q6：上传文件失败。**
确认上传目录 `uploadPath` 存在且有写权限，且 OpenResty `root` 目录权限为运行用户（如 `www-data`）。

---

## 9. 日志与回滚

- 后端日志：Spring Boot 输出到 `/var/log/admin.log`（或按 `logback.xml` 配置路径）。
- OpenResty 日志：`/usr/local/openresty/nginx/logs/access.log` / `error.log`。
- 前端为纯静态产物，保留上一版 `dist`/`build` 目录，升级时先备份再 `rsync --delete`，失败可一键切回。

---

## 10. 项目/模块源码索引清单

各项目/模块的 GitHub 仓库地址、分支及本机克隆目录如下：

| 项目 / 模块 | GitHub 仓库 | 分支 | 本机源码目录 |
| --- | --- | --- | --- |
| braight-aiclassroom（根工程：CMS 后台整体工程、教育官网及全部子服务） | `https://github.com/zfc2github/braight-aiclassroom.git` | main | `d:\IdeaProjects\git\braight-aiclassroom` |
| braightidea-education（教育官网前端） | `https://github.com/Manchi-L/braightidea-education.git` | education | `d:\IdeaProjects\git\braight-aiclassroom\braightidea-education` |
| braight-scratch3（Scratch3 图形化编程） | `https://github.com/zfc2github/braight-scratch3.git` | main | `d:\IdeaProjects\git\braight-aiclassroom\braight-scratch3` |

> 说明：
> - `braight-aiclassroom` 为整体工程仓库，含 `braightidea-edu-aiclassroom`（CMS 后端 + 前端）、`nodejs-proxy-server`、`quickdraw-online`、`creatability-seeing-music`、`Neural-Network-Visualisation`、`semi-conductor`、`eduStatics` 等模块。
> - `braightidea-education` 与 `braight-scratch3` 为独立仓库，分别对应官网前端与 Scratch3 编程站，构建产物见第 5 章对应章节。